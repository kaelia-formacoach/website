---
import Layout from "../layouts/Layout.astro";
---
<Layout>
  <section class="bg-white py-12"
  style="background-image: url('/images/index/liserai_quorum_left.svg'); background-repeat: no-repeat; background-size:auto;">
    <div class="container mx-auto px-6">
      <!-- Section Header -->
      <h1 class="text-4xl font-bold text-purple-800 mb-6 text-center fade-in">
        Rejoignez l'équipe KAELIA
      </h1>
      <p class="text-lg text-gray-700 mb-12 text-center fade-in">
        Vous êtes passionné(e) par la formation et souhaitez faire partie d’une équipe dynamique et engagée ? Chez KAELIA, nous croyons au pouvoir de l’apprentissage pour transformer les parcours professionnels et accompagner les organisations vers le succès.
      </p>

      <!-- Conteneur Flex pour les deux colonnes -->
      <div class="flex flex-col md:flex-row gap-8 mb-8">
        
        <!-- Colonne gauche : Pourquoi rejoindre KAELIA -->
        <div class="flex-1 slide-in-left">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Pourquoi rejoindre KAELIA ?</h2>
          <ul class="list-disc pl-6 space-y-2 text-gray-700">
            <li><strong>Un impact concret</strong> : En intégrant notre équipe, vous contribuez à former et inspirer des talents de tous horizons.</li>
            <li><strong>Une culture collaborative</strong> : Nous valorisons le travail d'équipe, la créativité et les initiatives personnelles.</li>
            <li><strong>Des opportunités de développement</strong> : Chez KAELIA, nous investissons dans nos collaborateurs avec des formations et des projets ambitieux.</li>
          </ul>
        </div>

        <!-- Colonne droite : Profils recherchés -->
        <div class="flex-1 slide-in-right">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Profils recherchés</h2>
          <p class="text-gray-700 mb-4">Nous sommes toujours à la recherche de talents motivés et curieux pour renforcer notre équipe :</p>
          <ul class="list-disc pl-6 space-y-2 text-gray-700">
            <li>Formateurs/trices experts dans leur domaine.</li>
            <li>Responsables pédagogiques et administratifs.</li>
            <li>Collaborateurs (communication, marketing, commercial, gestion et administration).</li>
          </ul>
        </div>

      </div>

      <!-- Phrase détachée -->
      <p class="text-center text-gray-700 font-bold text-lg mb-8 fade-in">
        Rejoignez-nous et participez à une aventure où la passion pour l'apprentissage fait la différence ! 🚀
      </p>

      <!-- Formulaire -->
      <div class="bg-gray-50 p-8 rounded-lg shadow-md fade-in">
        <h2 class="text-3xl font-semibold text-gray-900 mb-6">
          Postulez dès maintenant
        </h2>
        <form
          action="http://localhost:8000/mailer/submit_candid.php"
          method="POST"
          enctype="multipart/form-data"
          class="space-y-6"
        >
          <!-- Nom -->
          <div>
            <label for="name" class="block text-gray-700 font-semibold">Nom complet *</label>
            <input
              type="text"
              id="name"
              name="name"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
              required
            />
          </div>

          <!-- Email -->
          <div>
            <label for="email" class="block text-gray-700 font-semibold">Email *</label>
            <input
              type="email"
              id="email"
              name="email"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
              required
            />
          </div>

          <!-- Téléphone -->
          <div>
            <label for="phone" class="block text-gray-700 font-semibold">Téléphone</label>
            <input
              type="text"
              id="phone"
              name="phone"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
            />
          </div>

          <!-- CV -->
          <div>
            <label for="cv" class="block text-gray-700 font-semibold">CV (PDF) *</label>
            <input
              type="file"
              id="cv"
              name="cv"
              accept=".pdf"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
              required
            />
          </div>

          <!-- Lettre de motivation -->
          <div>
            <label for="cover_letter" class="block text-gray-700 font-semibold">Lettre de motivation (PDF) *</label>
            <input
              type="file"
              id="cover_letter"
              name="cover_letter"
              accept=".pdf"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
              required
            />
          </div>

          <!-- Message -->
          <div>
            <label for="message" class="block text-gray-700 font-semibold">Message *</label>
            <textarea
              id="message"
              name="message"
              rows="4"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500"
              placeholder="Expliquez brièvement pourquoi vous souhaitez nous rejoindre."
              required
            ></textarea>
          </div>

          <!-- Bouton de soumission -->
          <button
            type="submit"
            class="bg-purple-800 text-white px-6 py-3 rounded-lg shadow hover:bg-purple-700 transition"
          >
            Envoyer ma candidature
          </button>
        </form>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Initial State (hidden) */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 1s ease-out, transform 1s ease-out;
}

/* Visible State */
.fade-in.show {
  opacity: 1;
  transform: translateY(0);
}

.slide-in-left {
  opacity: 0;
  transform: translateX(-20px);
  transition: opacity 1s ease-out, transform 1s ease-out;
}

.slide-in-left.show {
  opacity: 1;
  transform: translateX(0);
}

.slide-in-right {
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 1s ease-out, transform 1s ease-out;
}

.slide-in-right.show {
  opacity: 1;
  transform: translateX(0);
}

</style>

<script>
  // Observer to detect when elements enter the viewport
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("show");
      }
    });
  });

  // Attach the observer to elements with animation classes
  const animatedElements = document.querySelectorAll(".fade-in, .slide-in-left, .slide-in-right");
  animatedElements.forEach((el) => observer.observe(el));
</script>